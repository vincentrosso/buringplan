# Copy this file to .env.local (gitignored) and fill in your key.
# Restrict the key by HTTP referrer in Google Cloud Console to localhost (dev)
# and https://<your-github-username>.github.io (prod) once deployed.
VITE_GOOGLE_MAPS_API_KEY=
