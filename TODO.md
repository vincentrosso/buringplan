# TODO

- [ ] Build unit/regression tests (frontend — no separate backend exists) to 80% coverage. Likely Vitest + React Testing Library, given the app is Vite/React; candidates for first coverage: `lib/geo.ts`, `lib/suggestedStops.ts`, `store/tripStore.ts`, `store/tripLog.ts`.
